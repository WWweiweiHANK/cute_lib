# 项目维护约定

- 此目录是独立工程，不修改上级目录中的其他游戏工程。
- 每轮实际修改都必须更新 CHANGELOG.md、运行相关验证、创建 Git 提交并推送到 origin/main。
- 源代码变更后重新构建，并把 dist 成品一起提交，保持双击启动可用。
- 不提交 node_modules、测试截图、日志、凭据或私人资料。
- 推送失败时保留本地提交并如实报告，不将本地提交称为已上传；不强制覆盖远程历史。

## 需求文档保存约定

- 每次在对话中为本项目形成一轮明确的需求说明、开发提示词或玩法实现规格，都必须在同一轮工作中同步保存到本仓库。
- 默认保存到 `docs/requirements/`，文件名使用 `YYYY-MM-DD-英文短名.md`；若该需求已有对应文档，则更新原文件而不是重复创建。
- 需求文档必须标注当前状态（设计需求 / 待实现 / 已实现）以及与现有系统的关系，避免把聊天内容与仓库实际进度混淆。
- 仅新增或调整需求文档时，不需要重新构建 `dist`；但仍需更新 `CHANGELOG.md` 并提交、推送到 `origin/main`。
- 当需求同时要求实际改代码时，需求文档与代码变更一并纳入该轮提交，并按本文件原有约定完成测试、构建和 `dist` 更新。
