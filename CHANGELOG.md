# 项目变更记录

按日期倒序记录。本文件仅记录 `NightLibrary-Counter` 独立项目。

## 2026-09-20 00:53 +08:00 — 跨交易书况记忆循环

- **修改目标**：让同一本实体书跨越借出、干扰业务与归还持续存在，玩家依靠观察记忆判断书况变化。
- **具体改动**：新增 mode=memory 和开始页「记忆循环测试」。林舟借带封面右下折角的《远方的灯塔》；离场后安静 5 秒，周宁借正常《城市里的雨》；再安静 10 秒，林舟归还同一实体及 Mesh，保留折角并脚本添加第三页咖啡污渍。顾客、证件与书沿用现有交互和镜头。质询各处后返回持书，可继续检查；最终放入归还托盘才结束。
- **涉及文件**：docs/requirements/2026-09-20-cross-transaction-memory-loop.md（保存本轮原始需求与实现状态）；src/memory-loop.js（新增书籍定义/实体、脚本序列和控制器）；src/transaction.js（逐损坏决定与汇总错误）；src/main.js（连续入场、模型缓存复用、状态重置、个别台词与 DEV 诊断）；src/assets.js（折角纹理、原 Mesh 清底重绘）；src/environment.js（复用托盘切换标牌）；index.html（记忆测试入口）；tests/memory-loop.mjs（新增四轮完整序列）；tests/transaction.test.js（实体延续、多损坏判断、拒借和重复决定）；tests/return-flow.mjs（改为显式收书结束）；tests/return-assets.mjs（折角与重复重绘验证）；README.md、docs/INTERACTION_FLOWS.md（规则、入口与持久化范围）；dist/index.html、dist/assets/index-Lxo94JS1.js（更新成品，替换旧哈希包）。
- **新增 / 删除 / 调整**：新增两份会话 BookInstance、分离外观定义与实体损坏/持有人，新增封面右下折角表现；复用原人物与书 Mesh，书况改变仅重绘纹理。取消还书单处赔偿后自动离场，独立还书入口同步使用逐处决定、托盘最终接受；保留借书/还书两个单项入口，不新增依赖或额外工作系统。
- **规则 / 策划变化**：每处 charge/waive 独立记录，重复决定更新同一项；最终 accept 汇总所有当前损坏，错罚旧损坏记 charged_existing_damage，新增损坏未罚记 missed_new_damage。允许不检查直接收书，无进度、旧/新标签、漏查提醒或历史查询。旧折角不追究仅代表该处判断正确，漏掉咖啡污渍仍使整笔错误。第一笔拒借则不生成虚假的第三笔归还。
- **技术实现变化**：CustomerSequenceController 用数据管理业务顺序、入场前变更与完成结果；借出保存损坏基线及持有人，归还时进入 IN_RETURN_TRANSACTION，结束后恢复 IN_LIBRARY 并保留全部损坏。正常渲染只拿显式白名单投影的视觉字段，不读取损坏来源。按 customerId / instanceId 复用模型；重绘前恢复清洁纹理，避免污渍累积。每笔重置持物、页码、记录锁与扫描器，共享原入场、离场、等待和声音系统。
- **兼容性或风险**：实体仅在当前页面会话持久化，刷新重置测试；既有本地交易记录不迁移或删改，新记录增添 bookInstanceId、damageDecisions、reason、errors，最终还书操作为 accept。仍沿用雨夜环境，不实现完整白天/夜晚。极端侧角度保留上一轮可见性约束。既存打包体积提示仍在，无新依赖。
- **验证结果**：先验证原状态机仍在单处决定后结束、页面缺少记忆数据，再实现；规则 11/11，四轮完整记忆路线（正确、错罚折角、放过污渍、完全不质询）与四条独立还书路线通过。验证实体/书 Mesh/人物模型一致、两段真实等待、期间转头、第三页污渍、决定后不提前记录、最终三条记录及正常入口隐藏诊断。四类纹理、重复重绘、遮挡、页码、侧角度验证通过；真实 Windows 启动和双层低音量环境声通过。构建通过，已检查借出折角与归还咖啡污渍截图；只读代码审查无 Important 问题。原借书三条路线、对话镜头、输入与精简 UI 回归全部通过，无页面异常。
- **待办事项**：本轮范围为三笔记忆循环；不扩展工资、房租、排队、闭馆、夜间行走或第二天。

## 2026-09-20 — 夜间整理书籍需求

- **修改目标**：保存闭馆后自由行走阶段的第一项夜间工作玩法：散落书籍拾取、检视、分类判断与书架归位。
- **具体改动**：新增 `docs/requirements/2026-09-20-night-shelving-flow.md`，规定 MAIN SORTING SHELVES 四分类、ShelfSlot、六本散落书、现有书架书可再次拾取、错误分类允许且不即时反馈、最终布局延迟到结算读取等完整 Codex 实现规格。
- **规则 / 策划变化**：整理阶段的完成条件是六本待整理书全部进入任意书架槽位，而不是全部放对；玩家可以重新拿出并调整，普通界面不显示正确率、剩余错误或分类答案。
- **验证结果**：本轮仅新增设计需求文档，不涉及运行时代码或 `dist`，无需重新构建。
- **待办事项**：待闭馆阅读转场与 NIGHT_FREE_ROAM 基础完成后实现本需求；下一阶段为闭馆清场。

## 2026-09-20 — 固化需求文档保存约定与闭馆阅读转场

- **修改目标**：把“每轮聊天形成的需求必须同步保存到对应 Git”写入项目维护约定，并保存最新的闭馆阅读转场需求。
- **具体改动**：新增 `docs/requirements/2026-09-20-closing-reading-transition.md`，定义最后顾客离开后通过私人书阅读推进时间、22:00 到点、玩家说“好了，下班了”、再执行 CLOSED / 锁门 / 灯光变化 / 起身并衔接夜间自由行动的完整规格；更新 `AGENTS.md`，规定后续需求 / 开发提示词默认同步写入 `docs/requirements/`。
- **规则 / 策划变化**：白天结束不再直接切夜晚；增加 `CLOSING_READING` 阶段。翻到新阅读页并停留达到阈值才推进时间，退出后保留阅读进度；到点后先由玩家角色说“好了，下班了”，随后才正式闭馆。
- **验证结果**：本轮只新增 / 更新 Markdown 需求与维护约定，不涉及运行时代码或 `dist`，因此无需重新构建游戏；已通过 Git 对象写入并以单次 fast-forward 提交更新 `main`。
- **待办事项**：该需求仍为待实现；实现后需补 `closing-reading` 浏览器测试并按既有流程构建、更新 `dist`。

## 2026-09-19 21:07 +08:00 — 还书检查与赔偿核心流程

- **修改目标**：在既有借书框架上完成检查归还书籍、指出损坏、交涉与决定的单笔交易闭环。
- **具体改动**：开始页新增借书/还书模式及两位还书访客，支持 mode=return&case=A/B。A 归还带右下封面划痕的《远方的灯塔》，B 归还无损坏的《城市里的雨》。复用点击持书、R 检视、拖动、三页翻阅；短按可见损坏后书移到右侧并保留 rotation、quaternion、页码及开合状态，0.7 秒聚焦顾客。质询后仅提供要求赔偿/不追究，两者直接收书并结束；也可持书点击左侧归还托盘直接接受。
- **涉及文件**：src/return-cases.js（新增案例）；src/transaction.js（还书状态、责任与结果）；src/main.js（模式接入、短按/拖动、损坏命中、姿态保留、轻对话、共享离场及诊断）；src/assets.js（两本封面、三类损坏 Canvas 纹理与表面 UV 热点）；src/environment.js（归还托盘标牌）；index.html、src/style.css（测试入口、轻决策及操作说明）；tests/return-flow.mjs、tests/return-assets.mjs（新增路线与视觉/姿态测试）；tests/transaction.test.js（扩充后台规则）；README.md、docs/INTERACTION_FLOWS.md（同步已实现范围与最新决策规则）；dist/index.html、dist/assets/index-BGpOGtys.js、dist/assets/index-XiY6g3xF.css（成品重建替换旧哈希包）。
- **新增 / 删除 / 调整**：新增还书交易与两项决定；新增划痕、污渍、撕裂/缺角的纹理绘制，不增加损坏网格或依赖。复用左侧木盘和顾客离场，取消旧设计中赔偿后继续检查/额外扫码/拒收的分支，本轮不实现该分支。
- **规则 / 策划变化**：短按不超过 250ms、6px 且起止同一损坏才质询，超阈值或长按后移动只旋转。正常接受损坏书仍允许，结果记错但不提示。旧损坏可见可问，但索赔记错；后台支持 existingDamageBeforeLoan。新旧损坏、责任与对错仅在后台记录/开发诊断，不给正常玩家答案。书始终保留在归还盘；付款只用轻纸张声。
- **技术实现变化**：Transaction 按类型选择状态表，共享物理动作通过 phase 复用。命中真实表面 UV，过滤背面、遮挡及非当前页；移书只改变位置，按选中点的可见性和朝向余弦收敛极侧角度的移动距离。展开书向右下稍远处让开脸部，近处阅读补光随侧持调整。开发诊断只读且需 dev=1；完成离场后只写一次记录，保留旧借书记录。
- **兼容性或风险**：桌面鼠标键盘范围；不改启动方式、环境声或环境灯光。撕裂以平面纹理表现，无纸张物理。极侧角度为了保留损坏可见会少移动，不强行转正。Three.js 打包超过 500kB 的既存提示仍在；无新增外网素材或依赖。
- **验证结果**：状态测试先失败再实现，最终 8/8 通过；成品四条还书路线（A 赔偿/放过/漏查接受、B 正常接受），验证短按与两种拖动、四元数保持、R/翻页、设置隔离及单次记录。开发夹具验证三类纹理、背面遮挡、非当前页不可点、污渍/撕裂第 1 页保持展开，以及真实约 75° 拖动后损坏仍可见。原有三条借书路线、输入回归、精简 UI、对话镜头、真实 Windows 启动及环境音测试通过，无页面异常；30 秒画面测试约 120 FPS，三种桌面尺寸目标可达。构建通过，检查侧持与展开书截图。只读审查发现的极侧角度遮挡已修复并复核。
- **待办事项**：本轮还书核心范围完成；工资、房租、排队、第二天、整理书架、超自然事件和赔偿金额均未扩展。

## 2026-09-19 19:54 +08:00 — 柜台式对话镜头

- **修改目标**：把普通字幕对话改为保持第一人称的克制镜头表演，让注意力自然落到人物与放物动作上。
- **具体改动**：普通 FOV 67°，对话 0.7 秒渐变至 62°并前移 3cm；对话鼠标输入 0.6 倍，偏移限制 ±2.5° / ±2°；开场脸→手/书→脸→手/证件，放物 0.5 秒低头 2°，收尾 0.5 秒恢复普通视角。扫码停留 0.25 秒再回看人物；拒借停留 0.3 秒、0.8 秒慢回看并留沉默；离场前恢复控制，不跟拍。
- **涉及文件**：src/main.js（镜头状态、渐变、剧情节拍、问答复用、只读诊断）；tests/dialogue-camera.mjs（新增镜头与借出/拒借验证）；tests/browser.mjs（等待继续提问的镜头交接）；README.md、docs/INTERACTION_FLOWS.md（参数和行为说明）；dist/index.html、dist/assets/index-BpqQU1qU.js（成品重建替换旧哈希包）。
- **新增 / 删除 / 调整**：新增 COUNTER_FREE / DIALOGUE_FOCUS / OBJECT_INSPECT 三种镜头状态；复用原动画，不增加框架或依赖；将结尾字幕拆成有停顿的短句；取消普通镜头贯穿全部对白的表现。
- **规则 / 策划变化**：站定后的对白会轻微吸引视线，但仍允许小幅转头；证件问答和递还复用聚焦；演出后恢复鼠标所指的普通方向，R 检视独立冻结镜头。借阅判断、物品操作及记录规则不变。
- **技术实现变化**：镜头与交易状态分开；平滑插值 FOV、前移与视线权重；人物焦点在对话开始时采样，离场不跟踪人物。手部低头与物品动画并行。
- **兼容性或风险**：不修改存档，不新增资源。桌面鼠标键盘验证；FOV 改变使人物与桌面占比略变，保持既有低多边形构图。已有 JS 包体积提示仍存在。
- **验证结果**：先验证旧版缺少镜头状态，再实现；开发版和最终生产包镜头测试通过，覆盖逐帧渐变、3cm 前移、跟随两次放物、对话转头、扫码/拒借停顿、R 检视和离场交接。生产包三条完整借阅路线通过、无页面异常；输入回归、精简 UI、状态规则 6/6 和构建通过。已检查对话看脸及放书截图；只读代码审查无 Important 问题。
- **待办事项**：本轮镜头交互完成；还书赔偿、阅读推进、自由行走整理书架仍按此前设计待实现。

## 2026-09-19 19:43 +08:00 — 修复等待转头与输入法下的 R 检视

- **修改目标**：等待客人入场时允许小幅转头，修复输入法按键事件导致 R 检视漏识别。
- **具体改动**：视线不再受交易 busy/started 限制，入场结束也不强制回正；检视按物理 KeyR 识别，保留原字符回退；Ctrl/Alt/Meta 组合键交还浏览器。
- **涉及文件**：src/main.js（视线与键盘事件）；tests/input-regression.mjs（新增输入回归）；README.md（行为与测试说明）；dist/index.html、dist/assets/index-CdzJeA8M.js（重建成品替换旧哈希包）。
- **新增 / 删除 / 调整**：新增一个可运行回归检查；移除转头的流程忙碌锁和入场回正；调整 R 的识别方式，无新依赖。
- **规则 / 策划变化**：等待、入场和对话期间都可小幅观察；中央书籍检视和菜单仍隔离场景输入。借书规则不变。
- **技术实现变化**：回归先在旧版本复现等待视线锁定和 key=Process/code=KeyR 无响应；修复共享输入入口，无新增状态层。
- **兼容性或风险**：不改存档。输入法验证使用浏览器 KeyboardEvent 模拟，不代表逐一实测所有系统输入法；英文键盘与大小写回退保留。构建仍有既存包体积提示。
- **验证结果**：生产包输入回归通过，覆盖入场前转头、不自动回正、IME 物理键、R 往返、翻页与设置隔离、组合键不触发检视；状态规则 6/6；三条完整借阅路线通过，无页面异常；构建与差异检查通过。
- **待办事项**：本轮两项修复已完成；其他工作模式仍沿用既有待办。

## 2026-09-19 19:33 +08:00 — 落实柜台借书交互

- **修改目标**：依照仓库 INTERACTION_FLOWS.md，将现有借书流程统一为左键拾取/使用、R 检视、滚轮翻页。
- **具体改动**：书籍拾取后在右手侧，R 往返中央检视，开合书均可拖动旋转；滚轮在封面和三页内容间前后切换，边界停止。证件右侧直接可读，照片/姓名/日期点击即提问；点击顾客选择「还给你」或「我还有问题」，递还后不再拾取。
- **涉及文件**：src/main.js（输入、持有位置、投影热点、问答、递还）；src/transaction.js（持有与递还状态）；src/assets.js（有效期、三页纹理）；index.html、src/style.css（移除核验单、新增热点和轻选择）；tests/transaction.test.js、browser.mjs、quiet-ui.mjs（交互验证）；README.md、docs/INTERACTION_FLOWS.md（操作说明、已实现范围）；dist（重建成品）。
- **新增 / 删除 / 调整**：新增证件三个可键盘聚焦的字段按钮、两项递还选择与三页内容；删除 E/F 操作、证件中央检视、桌面放回证件与核验勾选 UI/样式；Esc 统一用于设置，R 统一用于书籍检视。
- **规则 / 策划变化**：玩家通过借出/拒借作最终判断，不强制提问，也不自动识别正确证件；已递还证件不会在离场时重复取走。柜台保持固定座位与有限转头。
- **技术实现变化**：复用现有动画、状态机、Canvas 纹理和原生按钮，无新增依赖。证件热点按同一纹理坐标投影；翻页切换本地纹理，不增加实体页几何。
- **兼容性或风险**：旧 E/F 和 Esc 退出检视手势已替换，设置中提供新操作；保留旧存档字段 playerChecklistIdentity，新记录为 null，历史不改写。构建仍有现存 JS 体积提示。
- **验证结果**：先验证旧拾取状态不符合新规则，再修改；状态测试 6/6 通过。开发版及最终生产包均走通正常借出、不符证件拒借、不符证件借出；验证字段提问、继续提问/递还、R 往返、展开后旋转、翻页上下界、设置阻止输入、放回后拾取、记录时机与防重复，无浏览器异常。精简 UI 测试、真实 Windows 启动测试与构建通过；1024×768 的三个证件热点均完整位于屏幕内。只读代码复核无 Important 问题。
- **待办事项**：第 4–6 节还书破损/赔偿、阅读推进时间、自由行走整理书架尚未实现，文档已明确；本轮完成现有借书交互改造。

## 2026-09-18 — 固化借书、还书、阅读与整理书架交互

- **目的**：将已确认的核心交互整理成独立设计文档，统一后续实现口径，减少按键与重复交互。
- **新增**：`docs/INTERACTION_FLOWS.md`，覆盖借书、读者证核验、书籍检视、还书破损质询与赔偿决策、阅读过场、自由行走整理书架等流程。
- **交互原则**：左键负责拾取/使用，R 负责进入/退出书籍检视；检视中拖动旋转、滚轮翻页；物品默认进入右手持有位置。破损采用纹理 + 独立透明热点，不要求增加复杂几何。
- **还书规则**：点击破损后保持书籍当前姿态，并在同一构图中同时看到顾客与书；赔偿与拒收决策直接在对话中完成。顾客拒赔且玩家拒收时，本次还书直接失败，无需额外执行递还动作。
- **整理书架规则**：自由行走，点击书架具体位置放置；允许放错且无即时反馈，正确性延迟到最终结算。
- **验证**：本轮仅新增/更新 Markdown 设计文档，不涉及运行时代码与构建产物，因此无需重新构建游戏。

## 2026-09-18 — 接入 cute_lib 并首次上传

- **目的**：将独立工程完整上传到用户指定的 https://github.com/WWweiweiHANK/cute_lib ，后续每轮修改提交并推送。
- **变化**：配置 origin/main；合并并保留远程已有 LICENSE 与初始历史；源代码、成品、声音来源、说明与测试已首次推送成功（a54dc59）。
- **文件**：新增远程 LICENSE；更新本变更记录。游戏代码与成品没有变化。
- **验证**：合并无冲突；git diff --check 通过；首次 git push 成功并建立 main 跟踪关系。本轮仅仓库接入，不重复运行游戏测试。
- **维护**：后续沿用 origin/main，保留历史，不强制覆盖。首次登录与远程创建的旧待办已由现有 Git 认证和用户建立的仓库解决。
## 2026-09-17 — 本地启动修复、低照度与克制界面

- **修改目标**：修复 Windows 双击启动；简化画面与几何，降低雨声；将馆名收敛为公共机构标识，并落实每轮 Git 提交和推送。
- **具体改动**：ASCII/CRLF 启动入口改用 Node 原生静态服务，支持重复启动识别；减少角色、书页和书架细节，降低曝光、背景灯和木纹密度；移除多余标语；常驻仅小馆名与时间，任务和初次操作提示限时淡出，声音设置移入 Esc 菜单。
- **涉及文件**：启动图书馆.cmd、tools/launch.mjs、.gitattributes；src/main.js、environment.js、assets.js、audio.js、style.css、index.html；public/audio；tests/launch.test.mjs、audio.mjs、quiet-ui.mjs、browser.mjs；README.md、AGENTS.md、.gitignore、dist。
- **新增 / 删除 / 调整**：新增两份 CC0 环境声及来源记录、成品本地服务和相关验证；删除常驻底部操作栏、章节与任务信息堆叠、额外海报标语；调整场景主体约 8,400 至约 6,900 三角面。
- **规则 / 策划变化**：检查中 E 直接放回桌面；Esc 仍退出检查并继续手持，非检查时打开设置。其他借阅与自由判断规则保持原有实现。
- **技术实现变化**：播放器端口 5180，与开发端口 5178 分开；启动不依赖 npm 或联网。双层本地音频低通、交叉淡化、独立缓慢变化，加载失败保留轻底噪。源代码与构建成品一起纳入版本管理。
- **兼容性或风险**：需要 Node.js 22.12+、桌面 WebGL 2 浏览器。不同端口隔离本地交易记录。成品更新需先构建；JS 包体积提示仍存在，不影响构建。声音主观听感仍可通过设置调整。
- **验证结果**：构建通过；状态规则 6/6；真实 Windows 启动与重复启动通过；生产包三条完整借阅路线通过，无浏览器异常；精简 UI、提示消失、E 放回和 Esc 设置通过；音频真实解码为两层，平均 RMS 约 0.0016，静音约 0.000007。只读代码复核未发现重要问题。
- **待办事项**：GitHub 首次登录与远程创建进行中；本地验证完成后创建首个提交，登录成功后推送。此条不代表已经上传。
- **维护 / 回退**：每轮完成修改后更新记录、验证、提交并推送；独立工程不修改 Afterhours。测试截图与依赖缓存不提交。
## 2026-09-17 23:09 +08:00 — 新建夜阑图书馆柜台 Demo

- **修改目标**：根据用户提供的参考图和借阅流程说明，独立制作可在浏览器运行的单顾客借阅原型。未使用或修改原有 `Afterhours` 工程。
- **具体改动**：新增雨夜窗景、局部暖灯、木柜台、书架、空阅读区、开关入口、低多边形访客、可核对照片的借阅证、有厚度和可翻开封面的书、右侧扫码机、左侧待处理盘；访客步入、分别放物、等待、取物、离场；证件手动 NULL/PASS/FAIL 核验；旋转/翻开书；借出或拒借；静音开关与雨声、通风底噪、门铃、纸张和扫码反馈。
- **涉及文件**：`package.json`、`package-lock.json`（独立依赖与运行/构建/测试）；`index.html`、`src/style.css`（开始页、游戏提示、纸质清单、字幕与结算提示）；`src/main.js`（相机、射线、输入、动画、流程、记录与开发诊断）；`src/transaction.js`（纯交易状态机）；`src/assets.js`（人物 Profile、同源证件照片、书籍、纹理）；`src/environment.js`（场景、灯光、雨与地面反光）；`src/audio.js`（本地合成声音）；`tests/transaction.test.js`、`tests/browser.mjs`、`tests/visual.mjs`（规则、流程、画面与窗口尺寸验证）；`启动图书馆.cmd`（检测已有服务、缺失依赖安装、启动）；`README.md`、`.gitignore`、本文件（说明、生成物排除、记录）。
- **新增 / 删除 / 调整**：上述文件均为新增；没有删除既有工程文件。视觉验证后调整头发几何避免遮眼、补近距离纸面照明、降低柜台过曝、加强窗外冷蓝层次。
- **规则 / 策划变化**：保留水平 ±5° / 垂直 ±3° 平滑转头，选物使用独立光标射线加 E，解决固定中心射线无法触及左右设备的问题；拒借柜采用柜台内木质暂存盘。Esc 结束检查仍手持，R 可再次检查；设备不替玩家判断身份；提交不可改判或重复；正常界面不显示正确性。每次一名顾客，开始页切换两套案例。
- **技术实现变化**：Three.js 0.180、原生 ES Module、Vite；交易状态、场景、资产与声音分模块；重复书架书籍使用 InstancedMesh；一盏关键投影灯、低强度 Bloom、ACES、轻微暗角与颗粒。全部资产在本地生成，无 React 或大型游戏框架。
- **兼容性或风险**：面向桌面横屏、鼠标键盘、WebGL 2；没有手机触控流程。记录使用 `nightfall-transactions` 本地存储，保留最近 100 条；禁用存储时退回当前会话并提示，不中断流程。首次安装依赖需要网络，随后无外部素材请求。构建有单个 JS 包超过 500 kB 的体积提示（约 581 kB / gzip 154 kB），不影响构建成功。性能结果仅代表本机 Edge 测试环境。
- **验证结果**：状态机 6/6 测试通过；浏览器实际键鼠走通正常借出、不符证件拒借、不符证件借出三条路线；覆盖清单取消/互斥、证件放回、旋转书籍、封面开合、检查期间镜头冻结、继续手持、离场前不保存、离场后保存一次、重复输入不追加记录，未捕获页面异常。`npm run build` 通过；打包后页面已实测加载、访客入场、正常模式隐藏答案，无页面异常。30 秒静置无状态异常；1600×900、1366×768、1024×768 下全部操作目标在视野内；本机样本约 120 帧/秒，95 分位帧间隔 8.4 ms。独立只读代码审查没有发现 Critical/Important 问题，已补上其提出的持久化时机测试。截图与性能记录位于本地 `artifacts/`（不纳入源文件版本管理）。
- **待办事项**：无核心流程阻塞项。模型与声音为程序化原型；未逐像素复刻参考图，未进行多设备性能普测。还书、工资、第二天、排队、恐怖事件不在本轮范围内。
- **维护 / 回退**：工程完全独立，可单独移动或归档该目录；保留本地交易数据时勿清除对应浏览器站点存储。没有远程发布或上传。
